public class RectanglePerimeterAndArea extends RectangleArea {
    public RectanglePerimeterAndArea(double length, double breadth) {
        super(length, breadth);
    }
    public void calculatePerimeter() {
        double rectanglePerimeter = 2 * (getLength() + getBreadth());
        System.out.println("Rectangle Perimeter: " + rectanglePerimeter);
    }
}
public class RectangleArea {
    private double length;
    private double breadth;

    public RectangleArea(double length, double breadth) {
        this.length = length;
        this.breadth = breadth;
    }
    public void calculateArea() {
        double rectangleArea = this.length * this.breadth;
        System.out.println("Rectangle Area: " + rectangleArea);
    }
    public double getLength() {
        return length;
    }
    public double getBreadth() {
        return breadth;
    }
}
public class RectangleDemo {
    public static void main(String[] args) {
        System.out.println("Kumari Priya 079");
 
 RectanglePerimeterAndArea rectangle = new
 RectanglePerimeterAndArea(5.0, 3.0);
        rectangle.calculateArea();
        rectangle.calculatePerimeter();
    }
       }
